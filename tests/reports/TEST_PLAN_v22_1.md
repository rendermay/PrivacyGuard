# SecureRedact v22.1 测试计划

**测试日期:** 2026-02-09
**项目版本:** v22.1 - Viewport Filter
**修复内容:** 将事件过滤器从 `canvas_container` 移动到 `scroll.viewport()`

---

## 🔧 修复内容

### 问题诊断
- **原问题**: 右键点击完全无效，没有 [DEBUG] 日志输出
- **根本原因**: QScrollArea 的 `viewport()` 控件拦截了鼠标事件，导致事件过滤器无法捕获
- **解决方案**: 将事件过滤器安装到 `scroll.viewport()` 上

### 代码变更

#### 变更 1: 事件过滤器安装位置 (line 652)
```python
# 修改前 (v22.0)
self.canvas_container.installEventFilter(self)

# 修改后 (v22.1)
self.scroll.viewport().installEventFilter(self)
```

#### 变更 2: 事件过滤器对象检查 (line 520)
```python
# 修改前 (v22.0)
if obj == self.canvas_container and event.type() == event.Type.MouseButtonPress:

# 修改后 (v22.1)
if obj == self.scroll.viewport() and event.type() == event.Type.MouseButtonPress:
```

#### 变更 3: 版本号更新 (line 24)
```python
# 修改前
VERSION = "22.0 - Container Monitor"

# 修改后
VERSION = "22.1 - Viewport Filter"
```

---

## 🧪 测试步骤

### 阶段 1: 启动验证
1. 启动应用
2. 检查控制台输出，确认无错误
3. 确认窗口正常显示

### 阶段 2: 导入测试
1. 点击 "📂 打开" 按钮
2. 选择测试 PDF 文件
3. 确认 PDF 正常加载
4. 检查页面内容正常显示

### 阶段 3: 右键删除功能测试（重点）

#### 测试 3.1: 智能脱敏后的删除
1. 点击 "⚙️ 高级设置"
2. 启用 "身份证号" 和 "手机号码" 规则
3. 点击 "🔍 智能脱敏"
4. 等待处理完成
5. **在脱敏框上右键点击**
6. **验证**:
   - [ ] 控制台输出 `[DEBUG] MainWindow.eventFilter 捕获右键点击！`
   - [ ] 控制台输出 `[DEBUG] === 右键点击删除 (eventFilter) ===`
   - [ ] 脱敏框被成功删除
   - [ ] 页面刷新显示

#### 测试 3.2: 手动标记的删除
1. 在页面空白处拖拽绘制矩形框
2. **在手动标记的矩形框上右键点击**
3. **验证**:
   - [ ] 控制台输出 `[DEBUG]` 日志
   - [ ] 矩形框被成功删除
   - [ ] 页面刷新显示

#### 测试 3.3: 双页模式测试
1. 勾选 "📖 双页"
2. 在左侧页面的矩形框上右键点击
3. 在右侧页面的矩形框上右键点击
4. **验证**:
   - [ ] 左右两侧的删除功能都正常工作
   - [ ] 控制台正确显示点击的是哪个 canvas

### 阶段 4: 左键功能验证
1. 在页面空白处左键拖拽
2. **验证**:
   - [ ] 可以正常绘制矩形框
   - [ ] 控制台输出 `[DEBUG] 左键按下，开始绘制`
   - [ ] 没有重复触发绘制

### 阶段 5: 其他功能验证
1. 翻页功能
2. 缩放功能
3. 颜色切换
4. 导出功能

---

## ✅ 验收标准

### 核心功能
- [x] 代码修复完成
- [ ] 右键点击脱敏框可以成功删除
- [ ] 控制台有完整的 [DEBUG] 日志输出
- [ ] 左键点击不会重复触发

### 日志验证
预期看到以下日志：
```
[DEBUG] MainWindow.eventFilter 捕获右键点击！
[DEBUG] 点击位置 (相对container): (x, y)
[DEBUG] 左canvas几何: x=..., y=..., w=..., h=...
[DEBUG] 映射到左canvas: (x, y)
[DEBUG] === 右键点击删除 (eventFilter) ===
[DEBUG] 页面索引: 0
[DEBUG] 点击位置: (x, y)
[DEBUG] 手动框数量: N
[DEBUG] OCR框数量: M
[DEBUG] ✓ 删除XXX框: 页面0, 索引X
```

### 其他功能
- [ ] 智能脱敏正常工作
- [ ] 手动标记正常工作
- [ ] 导出功能正常工作
- [ ] 无控制台警告或错误

---

## 📝 测试结果

### 测试执行记录
- [ ] 阶段 1: 启动验证 - 通过/失败
- [ ] 阶段 2: 导入测试 - 通过/失败
- [ ] 阶段 3: 右键删除功能 - 通过/失败
- [ ] 阶段 4: 左键功能验证 - 通过/失败
- [ ] 阶段 5: 其他功能验证 - 通过/失败

### 发现的问题
1.
2.
3.

### 下一步行动
- 如果测试通过：执行完整的手动测试清单 (TESTING_GUIDE.md)
- 如果测试失败：分析问题原因，调整修复方案

---

**文档版本:** 1.0
**创建日期:** 2026-02-09
**适用版本:** SecureRedact v22.1 - Viewport Filter
