# SecureRedact Windows 错误日志 (2026-02-18)

## 错误信息

```
ImportError: DLL load failed while importing onnxruntime_pybind11_state: 动态链接库(DLL)初始化例程失败
```

## 完整堆栈跟踪

```
Traceback (most recent call last):
  File "main.py", line 13, in <module>
    from rapidocr_onnxruntime import RapidOCR
  File "pyimod02_importers.py", line 457, in exec_module
    File "rapidocr_onnxruntime\__init__.py", line 4, in <module>
    from .rapid_ocr_api import RapidOCR
  File "pyimod02_importers.py", line 457, in exec_module
    File "rapidocr_onnxruntime\rapid_ocr_api.py", line 13, in <module>
    from .utils import (LoadImage, init_args, UpdateParameters, concat_model_path,
  File "pyimod02_importers.py", line 457, in exec_module
    File "rapidocr_onnxruntime\utils.py", line 13, in <module>
    from onnxruntime import (GraphOptimizationLevel, InferenceSession,
  File "pyimod02_importers.py", line 457, in exec_module
    File "onnxruntime\__init__.py", line 78, in <module>
    raise import_capi_exception
  File "onnxruntime\__init__.py", line 26, in <module>
    from onnxruntime.capi._pybind_state import (
  File "pyimod02_importers.py", line 457, in exec_module
    File "onnxruntime\capi\_pybind_state.py", line 32, in <module>
    from .onnxruntime_pybind11_state import *  # noqa
    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
ImportError: DLL load failed while importing onnxruntime_pybind11_state: 动态链接库(DLL)初始化例程失败
```

## 环境信息

- **打包系统**: Windows (具体版本待确认)
- **Python 版本**: 3.11 (venv)
- **PyInstaller 版本**: 待确认
- **onnxruntime 版本**: 待确认
- **rapidocr_onnxruntime 版本**: 待确认

## 已尝试的解决方案 (2026-02-18)

### 1. 更新 PyInstaller Spec ✅
文件: `packaging/windows/config/SecureRedact_windows.spec`

修改内容:
- 增加从系统目录收集 VC++ DLL
- 增加从 Python 安装目录收集 VC++ DLL
- 新增关键 DLL: `vcruntime140_1.dll`, `msvcp140_1.dll`, `msvcp140_2.dll`

```python
# 方法1: 从 site-packages 收集
# 方法2: 从系统目录收集
# 方法3: 从 Python 安装目录收集
```

**结果**: ❌ 未解决

### 2. 创建启动器包装器 ✅
文件: `packaging/windows/scripts/launcher_wrapper.bat`

功能:
- 启动前检查必需的 DLL 文件
- 显示友好的中文错误提示

**结果**: ❌ 未解决 (打包后仍报错)

### 3. 修复 Batch 文件换行符 ✅
问题: Batch 文件使用 LF 换行符导致解析错误

修复:
```bash
python3 -c "
import glob
for filepath in glob.glob('*.bat'):
    with open(filepath, 'rb') as f:
        content = f.read()
    content = content.replace(b'\r\n', b'\n').replace(b'\r', b'\n')
    content = content.replace(b'\n', b'\r\n')
    with open(filepath, 'wb') as f:
        f.write(content)
"
```

**结果**: ✅ 修复了 batch 解析错误，但 DLL 问题仍未解决

---

## 下次尝试方案

### 方案 1: 分析 DLL 依赖
使用 Visual Studio 的 `dumpbin` 工具:
```cmd
dumpbin /dependents onnxruntime_pybind11_state.pyd
```

### 方案 2: 安装 VC++ Redistributable 到打包环境
在打包用的 Windows 机器上安装:
- https://aka.ms/vs/17/release/vc_redist.x64.exe

### 方案 3: 检查 onnxruntime 完整依赖
```python
import onnxruntime
print(onnxruntime.__file__)
# 检查该目录下的所有 DLL
```

### 方案 4: 使用 Dependency Walker
使用 Dependencies 工具分析:
- https://github.com/lucasg/Dependencies

### 方案 5: 尝试不同 onnxruntime 版本
```bash
pip uninstall onnxruntime onnxruntime-gpu
pip install onnxruntime==1.15.1
```

### 方案 6: 使用 --collect-binaries
在 spec 文件中使用更激进的收集方式:
```python
from PyInstaller.utils.hooks import collect_dynamic_libs
binaries = collect_dynamic_libs('onnxruntime', destdir='onnxruntime/capi')
```

---

## 相关资源

- GitHub Issue: https://github.com/microsoft/onnxruntime/issues/16113
- PyInstaller Issue: https://github.com/pyinstaller/pyinstaller/issues/6925
- VC++ Redist: https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist

---

## 备注

- 当前 macOS 版本工作正常
- Windows 版本需要解决此 DLL 问题才能发布
- 考虑在干净的 Windows 虚拟机中测试
