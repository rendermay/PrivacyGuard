# SecureRedact v22.1 修复说明

## 📋 本次修复内容

### 问题
- 右键点击脱敏框完全无效
- 控制台没有任何 [DEBUG] 日志输出

### 根本原因
QScrollArea 的控件层级结构：
```
MainWindow
  └── scroll (QScrollArea)
        └── viewport()  ← 鼠标事件实际到达这里
              └── canvas_container (QWidget)
```

**原问题**: v22.0 将事件过滤器安装在 `canvas_container` 上，但鼠标事件在到达 `canvas_container` 之前就被 `viewport()` 拦截了。

### 解决方案
将事件过滤器安装到 `scroll.viewport()` 上，这是真正接收鼠标事件的控件。

## 🔧 代码变更

### 1. 事件过滤器安装位置 (main.py:652)
```python
# v22.0 (错误)
self.canvas_container.installEventFilter(self)

# v22.1 (正确)
self.scroll.viewport().installEventFilter(self)
```

### 2. 事件过滤器检查 (main.py:520)
```python
# v22.0 (错误)
if obj == self.canvas_container and event.type() == event.Type.MouseButtonPress:

# v22.1 (正确)
if obj == self.scroll.viewport() and event.type() == event.Type.MouseButtonPress:
```

### 3. 版本号更新 (main.py:24)
```python
VERSION = "22.1 - Viewport Filter"
```

## 🧪 测试步骤

### 快速验证（核心功能）

1. **启动应用**
   ```bash
   cd /Users/a49144/Desktop/临时coding/PrivacyApp
   source venv/bin/activate
   python main.py
   ```

2. **导入测试 PDF**
   - 点击 "📂 打开" 按钮
   - 选择 `test_sample.pdf`
   - 确认 PDF 正常加载

3. **测试智能脱敏**
   - 点击 "⚙️ 高级设置"
   - 勾选 "身份证号" 和 "手机号码"
   - 点击 "🔍 智能脱敏"
   - 等待处理完成

4. **测试右键删除功能** ⭐ 重点
   - 在脱敏框上**右键点击**
   - **预期结果**:
     ```
     [DEBUG] MainWindow.eventFilter 捕获右键点击！
     [DEBUG] 点击位置 (相对container): (x, y)
     [DEBUG] 左canvas几何: x=..., y=..., w=..., h=...
     [DEBUG] 映射到左canvas: (x, y)
     [DEBUG] === 右键点击删除 (eventFilter) ===
     [DEBUG] 页面索引: 0
     [DEBUG] 点击位置: (x, y)
     [DEBUG] 手动框数量: N
     [DEBUG] OCR框数量: M
     [DEBUG] ✓ 删除XXX框: 页面0, 索引X
     ```
   - **视觉验证**: 脱敏框被成功删除

5. **测试手动标记删除**
   - 在页面空白处拖拽绘制矩形框
   - 在矩形框上右键点击
   - 验证矩形框被删除

6. **测试左键功能**
   - 在页面空白处左键拖拽
   - 验证可以正常绘制矩形框
   - 验证没有重复触发

7. **测试双页模式**（可选）
   - 勾选 "📖 双页"
   - 在左右两侧分别测试右键删除功能

## ✅ 验收标准

- [x] 代码修复完成（事件过滤器安装在 viewport 上）
- [ ] 右键点击脱敏框可以成功删除
- [ ] 控制台输出完整的 [DEBUG] 日志
- [ ] 左键点击不会重复触发
- [ ] 智能脱敏功能正常
- [ ] 手动标记功能正常
- [ ] 其他功能无异常

## 📝 测试报告模板

### 测试环境
- 操作系统: macOS
- Python 版本: 3.11.14
- 应用版本: v22.1 - Viewport Filter

### 测试结果
- [ ] 应用正常启动
- [ ] PDF 导入成功
- [ ] 智能脱敏成功
- [ ] **右键删除功能正常** ← 核心功能
- [ ] 手动标记删除正常
- [ ] 左键绘制正常
- [ ] 双页模式正常

### 发现的问题
1.
2.
3.

### 控制台日志
```
（粘贴相关的 [DEBUG] 日志）
```

## 🚀 后续步骤

如果测试通过：
1. 执行完整的手动测试清单 (TESTING_GUIDE.md)
2. 更新版本号到 v20.0（正式版本）
3. 更新 CHANGELOG
4. 重新打包应用

如果测试失败：
1. 记录失败现象和日志
2. 分析问题原因
3. 调整修复方案

---

**修复日期**: 2026-02-09
**修复版本**: v22.1 - Viewport Filter
**文件路径**: `/Users/a49144/Desktop/临时coding/PrivacyApp/main.py`
