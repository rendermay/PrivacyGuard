---
phase: 3
slug: 03-word
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-11
---

# Phase 3 — Word 文档接入识别引擎（双栏对比预览自动高亮）UI Design Contract

> Visual and interaction contract for Phase 3 — `privacyguard/pii/word_adapter.py` + Word 双栏对比预览 PII 自动高亮 + 工具栏「本文件使用全遮蔽」toggle。Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none（PyQt6 + QtWebEngine 桌面应用，无 shadcn / 第三方组件库） |
| Preset | not applicable |
| Component library | PyQt6 原生 widget + QWebEngineView 内嵌 HTML/CSS（双栏预览走 inline style + class） |
| Icon library | PyQt6 原生 `QStyle.StandardPixmap`（沿用 main.py 既有 `get_icon` 调用，无新增图标库） |
| Font | `'Segoe UI Variable', 'Segoe UI', 'Microsoft YaHei UI', 'Microsoft YaHei', Arial, sans-serif`（`theme.py::Theme.FONT_FAMILY`，Phase 3 零修改） |

> **Rationale:** Phase 3 是 desktop Qt 应用，不存在 React/shadcn/Tailwind 概念。所有视觉契约以 **Qt widget stylesheet + 双栏预览 inline CSS** 为落点，不引入新设计语言、不引入新字体、不引入新色系。

---

## Spacing Scale

Qt layout 内 spacing 与 HTML inline style padding 都按以下 4 倍数 token 收敛：

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | 工具栏 toggle 控件内边距；tooltip 箭头与文字间距 |
| sm | 8px | 高亮 mark 上下 padding；列表项纵向间距 |
| md | 16px | 工具栏按钮水平间距；预览区段间垂直 padding |
| lg | 24px | 双栏左右栏间距；modal/sheet 边距 |
| xl | 32px | 主面板与子面板分隔 |
| 2xl | 48px | （保留）大段分隔；Phase 3 不强制使用 |
| 3xl | 64px | （保留）页面级；Phase 3 不强制使用 |

Exceptions:
- 高亮 mark 的 `padding` 走 `0 2px`（HTML 内联样式紧贴文字基线，避免影响段高）
- 工具栏 toggle 控件的最小命中区走 Qt 默认 22×22（不强制 44px，桌面端遵守 OS 习惯）
- tooltip 与目标高亮之间的间距走 6px（不上 8 倍数轴，保持视觉贴合）

---

## Typography

Qt 端（main.py / workers）与 HTML 端（QWebEngineView 双栏预览）共用以下三档：

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px | 400 | 1.5 | Qt 控件 / 段内文字 / 工具栏标签 |
| Label | 12px | 600 | 1.3 | tooltip 头部 / 高亮 mark `title` 属性 |
| Heading | 16px | 600 | 1.4 | 候选计数标签 / 工具栏 toggle 旁的实体类型标签 |

> **Font stack 锁定:** Qt 端走 `Theme.FONT_FAMILY`；HTML 端双栏预览走 `'Microsoft YaHei UI', 'Microsoft YaHei', 'Segoe UI', Arial, sans-serif`（与既有 cp27 修复点的预览内 font-family 一致，不引入 web font）。
> **Font weight 锁定:** 400（regular）+ 600（semibold）二档；不引入 700/800，避免视觉杂乱。
> **HTML 高亮 mark 内:** 继承段内 `font-family` / `font-size`；高亮自身不另设字号，仅设背景色 + 边框 + 圆角。

---

## Color

### 60/30/10 视觉分布

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF`（浅色）/ `#1E2836`（深色） | 主面板背景、QWebEngineView 双栏 preview 容器底色 |
| Secondary (30%) | `#F7F8FA`（浅色）/ `#151C26`（深色） | 工具栏、info bar、scroll area 底色 |
| Accent (10%) | `#D64545`（浅色）/ `#FF6B6B`（深色） | **仅用于 PII 命中在左栏（原文）的高亮边框 + mask 替换区底色**——见下文「Accent reserved for」 |
| Destructive | `#D64545`（浅色）/ `#FF6B6B`（深色） | 「本文件使用全遮蔽」toggle 激活态、`btn_mask_override` 文档级 toggle 激活态 |

### Accent reserved for（仅以下元素，绝不外溢）

1. **左栏（原文）PII 命中 mark 的 `border` 颜色** — `border: 1px solid #D64545;`
2. **左栏 PII 命中 mark 的 `box-shadow` 颜色** — `box-shadow: 0 0 0 1px rgba(214,69,69,0.25);`
3. **右栏（替换后）PII 命中区 mask 文本底色** — `background: rgba(214,69,69,0.12); border: 1px solid #D64545;`
4. **PII 高亮 mark 的 `outline`（focus 态）** — `outline: 2px solid #D64545;`
5. **工具栏「本文件使用全遮蔽」toggle 激活态边框** — 与 PDF 端 `btn_mask_override` 共用

### 与 PDF 端深红色一致性

- Phase 1 PDF 端 PII 自动识别用 `#d63031` 作高亮色；Phase 3 **不引入新色**，统一走 theme.py `danger` 槽 `#D64545`（浅色）/ `#FF6B6B`（深色）
- 跨格式识别结果在用户视觉认知中统一为「红色 = 自动识别的敏感项」，不出现「PDF 红 / Word 紫」分色
- HTML 高亮 mark 通过 CSS 变量而非硬编码，便于 Phase 7 切换主题时一处改全局生效

### OCR / rule / manual 三类既有高亮的色槽（Phase 3 零修改）

| Source | 颜色（浅色） | 颜色（深色） | 来源 |
|--------|-------------|-------------|------|
| ocr | `#FFEB3B`（黄底） | `#FFD54F` | 既有 `ocr-highlight` class，Phase 3 不改 |
| rule | `#A5D6A7`（绿底） | `#81C784` | 既有 `rule-highlight` class，Phase 3 不改 |
| manual | `#90CAF9`（蓝底） | `#64B5F6` | 既有 `manual-highlight` class，Phase 3 不改 |
| **pii**（新增） | `#D64545`（红底 + 红边框） | `#FF6B6B`（红底 + 红边框） | `pii-highlight` class，D-01 扩展源 |

---

## Copywriting Contract

> Phase 3 候选审阅仅做「双栏高亮」最小形态（UX-01/UX-02 完整 UI 推 Phase 7）。以下文案适用于工具栏 toggle、tooltip、错误条带与右下角状态提示。

| Element | Copy |
|---------|------|
| Primary CTA | `保存脱敏文档`（沿用 main.py 既有 `btn_save_word`，Phase 3 不改文案） |
| Toolbar toggle — 标签 | `本文件使用全遮蔽`（与 PDF 端 `btn_mask_override` 同文案；位置紧邻摆放） |
| Toolbar toggle — tooltip | `切换后本文档所有 PII 命中按全黑框 [已脱敏] 写入产物；不影响其他文件。`（中文为主语在前动词在后的祈使句，16 字以内） |
| Toolbar toggle — 激活态描述 | `全遮蔽模式已开启`（status bar 显示） |
| 左栏 PII 高亮 mark `title` 属性（hover tooltip） | `[PII] {entity_type} · {confidence_tier} · {source}`（如 `[PII] CN_ID_CARD · HIGH · text`） |
| 右栏 PII 替换区 hover tooltip | `[已脱敏] {entity_type} · mask: {mask_strategy}`（如 `[已脱敏] CN_ID_CARD · mask: 110101********1234`） |
| Empty state — 双栏加载但无 PII 命中 | status bar: `本次扫描未发现敏感信息`（沿用既有 status bar 文案，14 字以内） |
| Empty state — Word 文档未打开 | status bar: `请先打开 Word 文档`（既有文案，Phase 3 零修改） |
| Empty state — PII 引擎未启用 | status bar: `PII 自动识别未启用，请在设置中开启`（沿用既有 `pii_settings.engine_enabled=False` 提示文案） |
| Error state — OCR worker 失败 | status bar: `扫描失败：{error_message}。请重试或取消。`（沿用既有 status bar 错误格式） |
| Error state — PII 引擎异常 | status bar: `PII 引擎异常：{error_message}。本次仅展示规则命中。`（**新增**——Phase 3 引入 PII 路径后必须区分两类错误，避免用户混淆） |
| Error state — 跨 run 样式丢失警告 | status bar: `警告：{n} 处 run 级格式（粗体/斜体）已被合并段级样式替代。`（**新增**——D-07 决策产物，真脱敏时若存在跨 run 命中触发） |
| Destructive confirmation | 无新增破坏性确认；「本文件使用全遮蔽」toggle 是文档级非破坏性切换，不弹确认框（与 PDF 端 `btn_mask_override` 一致） |

---

## UI Considerations

> Phase 3 的 UI 形态是 Qt 工具栏 toggle + 双栏 preview DOM patch 高亮，无独立侧栏审阅面板。状态覆盖如下：

Applicable state considerations resolved: 5 covered, 3 backstop, 0 unresolved

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | 双栏预览（左/右栏均无 PII 命中） | ✅ covered | 左栏无 `pii-highlight` mark；右栏无 mask 替换区；status bar 显示「本次扫描未发现敏感信息」 |
| empty | Word 文档未打开 | ✅ covered | status bar 显示「请先打开 Word 文档」；工具栏 toggle disabled（Qt `setEnabled(False)`） |
| empty | PII 引擎未启用（`pii_settings.engine_enabled=False`） | ✅ covered | status bar 显示「PII 自动识别未启用」；双栏 preview 仅渲染 rule / manual / ocr 三层高亮 |
| error | PII 引擎异常（validators 加载失败 / rules.json 缺失） | ✅ covered | status bar 显示「PII 引擎异常：{msg}」；左/右栏不渲染 pii-highlight，仅渲染其余三层 |
| error | 跨 run 样式丢失（D-07 run 合并副作用） | ✅ covered | 真脱敏保存后 status bar 显示「警告：N 处 run 级格式已被合并段级样式替代」；产物已写入（真脱敏底线 > 格式完美） |
| long-text | 段文本 > 5000 字（PDF 端常见超长段） | 🧪 backstop | 视觉回归测试用例 `test_word_preview_highlight.py::test_long_paragraph_pii_highlight_no_overflow` 验证 mark `data-end` 不越界；DOM patch 不溢出段边界 |
| overflow | 单段 PII 命中数 > 20 个（test_data 中刻意构造） | 🧪 backstop | 视觉回归测试 `test_word_preview_highlight.py::test_many_pii_hits_in_single_paragraph` 验证 mark 不重叠、不破坏段高；hover tooltip 仍可读 |
| partial | 跨 run 命中（同一 PII 文本跨多个 run 拼接） | 🧪 backstop | 视觉回归测试 `test_word_preview_highlight.py::test_pii_hit_across_runs` 验证 char_offset 正确；mark 不被 run 边界切断 |

<!-- Phase 3 没有"loaded list / pagination / search"等动态列表场景，loading 状态由 Qt 原生 QProgressBar 接管，error 状态走 status bar 文案，不另立"loading"行。 -->

---

## Component Inventory（Phase 3 落地清单）

> 此段不属于 GSD UI-SPEC 标准模板，是 Phase 3 作为 desktop 应用特有的实施清单，给 planner/executor 直接照表实现。

### 1. 新增 QWebEngineView HTML 类（双栏预览 PII 高亮）

| 类名 / 入口 | 文件 | 职责 |
|------------|------|------|
| `.pii-highlight` CSS class | `theme.py` 或 `main.py:_get_word_preview_css` | 左栏（原文）PII 命中 mark 的样式 |
| `.pii-mask-highlight` CSS class | 同上 | 右栏（替换后）PII mask 替换区的样式 |

CSS 规格（直接落入 Qt 端 `setHtml` 的 `<style>` 块，cp27 修复点附近）：

```css
/* 左栏原文 PII 高亮（与 PDF 端 PII 同色 #D64545 / #FF6B6B） */
.pii-highlight {
    background: rgba(214, 69, 69, 0.12);
    border: 1px solid #D64545;
    border-radius: 3px;
    padding: 0 2px;
    cursor: help;
    box-shadow: 0 0 0 1px rgba(214, 69, 69, 0.18);
}

/* 深色主题（dark mode） */
[data-theme="dark"] .pii-highlight {
    background: rgba(255, 107, 107, 0.15);
    border-color: #FF6B6B;
    box-shadow: 0 0 0 1px rgba(255, 107, 107, 0.22);
}

/* 右栏替换后 PII mask 区 */
.pii-mask-highlight {
    background: rgba(214, 69, 69, 0.08);
    border: 1px solid #D64545;
    border-radius: 3px;
    padding: 0 2px;
    font-weight: 600;
}

[data-theme="dark"] .pii-mask-highlight {
    background: rgba(255, 107, 107, 0.10);
    border-color: #FF6B6B;
}

/* Focus / hover 态 */
.pii-highlight:hover,
.pii-highlight:focus {
    outline: 2px solid #D64545;
    outline-offset: 1px;
}
```

### 2. 工具栏「本文件使用全遮蔽」toggle

| 控件 | 文件 | 复用 / 新增 | 视觉契约 |
|------|------|------------|---------|
| `btn_mask_override` | `main.py`（既有 PDF 端 5896-5901） | **复用同一 widget** | D-05 决策：与 PDF 共用；视觉上紧邻摆放，控件不复制 |
| `self._word_mask_override_this_doc` | `main.py`（独立字段，**新增**） | 新增 | Phase 3 改为独立字段而非 `word_data[0][...]`，避免 word_data 业务键空间污染（research.md Open Question #1 已 raise） |

视觉契约：
- 控件位置：紧邻 PDF `btn_mask_override`（同一 toolbar group 内，相距 8px）
- 未激活态：Qt 默认 toggle 样式 + 文本「本文件使用全遮蔽」
- 激活态：`background: #D64545; color: #FFFFFF; border: 1px solid #D64545;`（浅色）/ `#FF6B6B`（深色）
- tooltip：见上「Copywriting Contract」
- toggle 触发时写 `self._word_mask_override_this_doc = "blackout" | "partial"`，**不** 写 word_data 业务键
- `MainWindow._save_word` 调用 `apply_pii_replacements_to_docx` 时按 `self._word_mask_override_this_doc` 决定 `mode` 形参（默认 `"partial"`）

### 3. 双栏预览 PII 数据注入路径

| 接入点 | 文件 | 职责 |
|--------|------|------|
| `_build_word_original_panel_updates` | `main.py:11940-11952` | 左栏原文高亮生成；D-01 注入 `pii_matches=word_data[key]["pii"]` |
| `_build_word_replaced_panel_updates` | `main.py:11986-11998` | 右栏替换后高亮生成；同上 |
| `merge_word_matches_with_priority` | `main.py:863-906` | **新增** `pii_matches: Optional[List[PIIHit]] = None` 形参；`ocr + pii` 同层去重（D-02: PII 后追加，校验位质量 > OCR 胜出） |
| `_apply_word_panel_updates` | `main.py:12000-12005`（cp27 修复点） | DOM patch 写入；PII 数据按 `data-key` 局部更新，**不破坏** cp27 修复边界 |

### 4. 状态条带（status bar）文案接入

| 文案 key | 触发条件 | 来源 |
|---------|---------|------|
| `本次扫描未发现敏感信息` | scan 完成且 `word_data[key]["pii"]` 全部为空 | 既有文案，Phase 3 复用 |
| `PII 自动识别未启用，请在设置中开启` | `pii_settings.engine_enabled == False` | 既有文案 |
| `PII 引擎异常：{msg}。本次仅展示规则命中。` | `collect_pii_word_hits` 抛异常 | **新增**——Phase 3 引入 PII 路径必须区分 OCR 失败 vs PII 失败 |
| `警告：{n} 处 run 级格式已被合并段级样式替代` | `_save_word` 真脱敏时 `len(run_lost_format) > 0` | **新增**——D-07 run 合并副作用提示 |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none（Phase 3 非 web 项目，无 shadcn） | not required |
| Third-party registries | none（Phase 3 零新增外部依赖，零新 npm/pip 包；PyInstaller hiddenimports 同步是 `PrivacyGuard_windows.spec` 文本编辑，不涉及第三方 block 注入） | not required |

> PyInstaller hiddenimports 同步（D-14）属于构建配置范畴，不是 shadcn registry 概念，不在 Registry Safety 列内追踪。`packaging/windows/config/PrivacyGuard_windows.spec` 的 `privacyguard_hiddenimports.extend([...])` 列表新增 `'privacyguard.pii.word_adapter'` 一行是 code edit，由 D-14 + Wave 3 Plan 4 任务承接。

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
